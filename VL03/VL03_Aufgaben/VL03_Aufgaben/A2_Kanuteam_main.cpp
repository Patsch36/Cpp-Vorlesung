/*
Implementiert folgendes in einer nicht-main-Funktion:?

Ein Kanuteam besitzt ein Boot. Ein Kanuteam besteht aus bis zu 5 Personen. Personen können zu bis zu 3 Kanuteams gehören. 
Jedes Boot gehört 0 bis einem Team. Davon abgesehen haben alle 3 Klassen nur zwei Properties, einen Namen und eine hochzählende ID. 
Erstellt für jedes Objekt einen Konstruktor, der eine Ausgabe erzeugt „Erstelle *Typ* *id*“ und einen Destruktor der schreibt „Zerstöre *Typ* *id*“. 
Alle Objekte können unabhängig voneinander erstellt und den anderen zugewiesen werden?

Legt mehrere Instanzen der verschiedenen Klassen an und weißt sie untereinander zu. Kontrolliert, dass die gleiche Menge Objekte angelegt und zerstört wird.?

Implementiert es einmal mit und einmal ohne Smartpointer
*/

int main()
{

	return 0;
}